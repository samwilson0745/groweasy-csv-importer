# URL of the backend API (see backend/.env.example for the server config)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional: how long (ms) to wait for the /confirm import call before giving
# up client-side. Defaults to 120000 (2 minutes) if unset.
NEXT_PUBLIC_API_TIMEOUT_MS=120000
